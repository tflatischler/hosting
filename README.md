# hosting

A self-contained landing page for a static host. It shows a collapsible data tree and a searchable grid of page cards, all driven by a single sites.json manifest. Built to run on GitHub Pages with no build tooling.